<div class="col-md-4">

  <div class="card" style="width: 18rem;">
    <img src="{{this.images.0.url}}" class="card-img-top" alt="{{this.name}}" />
    <div class="card-body">
      <h5 class="card-title">{{this.name}}</h5>
    </div>
    <div class="card-body">
      <a href="/albums/{{this.id}}" class="card-link">View Albums</a>
    </div>
  </div>
</div>